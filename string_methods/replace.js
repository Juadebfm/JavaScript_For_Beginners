// replace(): takes as a parameter the old substring and new substring and replaces the old with the new.
// string.replace(oldsubstring, newsubstring)

let string = "JavaScript is fun";
console.log(string.replace("is fun", "is awesome")); // JavaScript is awesome
let country = "Nigeria";
console.log(country.replace("ia", " ")); // Niger
